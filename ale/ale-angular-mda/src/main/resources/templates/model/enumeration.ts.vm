
/**
 * Enumeration for Angular Frontend for ${enumeration.name}.
 *
 * Generated Code - DO NOT MODIFY
 */
export enum ${enumeration.name} {
#foreach ($enum in $enumeration.enums)
#if($enumeration.isValued())
    ${enum.name} = ${enum.value},
#else
    ${enum.name} = '${enum.name}',
#end
#end
}
