//
//  ${entityName}.h
//  ${projectName}
//
//  THIS FILE IS GENERATED -- DO NOT MODIFY
//  Copyright (c) 2014 ASK, LLC. All rights reserved.
//

#import <Foundation/Foundation.h>
#foreach ($import in $imports)
#import "${import}"
#end

@interface ${entityName} : NSObject

#foreach ($idField in $idFields)
@property (${idField.typeAttributes}) ${idField.type} ${idField.typeReferenceAttribute}${idField.name};
#end
#foreach ($field in $fields)
@property (${field.typeAttributes}) ${field.type} ${field.typeReferenceAttribute}${field.name};#if ($field.isEnumeration()) // Values must be ${field.enumerationType} constants${lineSeparator}#else${lineSeparator}#end
#end
#foreach ($reference in $references)
@property (${reference.typeAttributes}) ${reference.type} *${reference.name};
#end
#foreach ($relation in $relations)
@property (${relation.typeAttributes}) NSSet *${relation.label}s; // Elements must be of type ${relation.type}
#end

@end
