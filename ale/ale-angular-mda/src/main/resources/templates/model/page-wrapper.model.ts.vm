import { PageWrapperBase } from '../../generated/model-base/page-wrapper-base.model';

export class PageWrapper<T> extends PageWrapperBase<T> {
  constructor(pageWrapper?: PageWrapper<T>) {
    super(pageWrapper);
  }
}
