package ${basePackage}.service;

import java.util.Collection;
import java.util.Iterator;

import ${basePackage}.transfer.${entity.name};

/**
 * An iterator that returns instances of ${entity.name}s
 *
 * Generated Code - DO NOT MODIFY
 */
public class ${entity.name}Iterator {

	private Iterator iterator;
	
	public ${entity.name}Iterator(Collection entities) {
		iterator = entities.iterator();
	}
	
	/**
	 * Answers whether there are more ${entity.name}s
	 *
	 * @return boolean - More or not
	 */
	public boolean hasNext() {
		return iterator.hasNext();
	}

	/**
	 * Answers the next ${entity.name} in the collection
	 *
	 * @return ${entity.name} - The next ${entity.name}
	 */
	public ${entity.name} next() {
		return (${entity.name}) iterator.next();
	}

}
