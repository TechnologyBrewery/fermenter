package ${basePackage}.service.ejb;

#foreach( $import in $service.imports )
import $import;
#end

import java.rmi.RemoteException;
import javax.ejb.EJBObject;

/**
 * Remote interface for the ${service.name} service
 *
 * Generated Code - DO NOT MODIFY
 */
public interface ${service.name}ServiceRemote extends EJBObject {

#foreach( $operation in $service.operations )
#if ($operation.isRemote())

	/**
	 * Execute the ${operation.name} service
	 *
	 * @return ${operation.responseType}
	 * @throws RemoteException This will never be thrown in a Toolkit-based application unless an Weblogic-level
	 * is encountered
	 */
	public ${operation.responseType} ${operation.lowercaseName}(${operation.signature}) throws RemoteException;

#end	 
#end
}