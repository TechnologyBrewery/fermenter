package ${basePackage}.persist;

import ${entity.identifier.import};

import ${basePackage}.bizobj.${entity.name}BO;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.JpaSpecificationExecutor;

/**
 * Data access repository for the ${entity.name} business object.
 * 
 * GENERATED STUB CODE - PLEASE *DO* MODIFY
 *
 * Originally Generated from: ${templateName}
 */ 
public interface ${entity.name}Repository extends JpaRepository<${entity.name}BO, ${entity.identifier.javaType}>, JpaSpecificationExecutor<${entity.name}BO> {
	
	/**
	 * Developers should leverage this interface to define any query logic
	 * that cannot be realized through {@link JpaRepository}'s built-in
	 * functionality.  
	 */

}